// new index (1).html (داخل وسم <script>)
// (يجب تعريف هذا المتغير في النطاق العلوي لملف JS)
let appliedCodeId = null; 

// ===================================
// 🎫 التحقق من الكود (Player Flow)
// ===================================

window.validateVoucher = async function(fieldId) {
    const codeValue = document.getElementById('voucherCodeInput').value.trim();
    const resultContainer = document.getElementById('voucherResult');
    
    if (!codeValue) {
        resultContainer.innerHTML = '';
        appliedCodeId = null;
        // يجب هنا استدعاء دالة لتحديث ملخص الحجز وإعادة حساب الإجمالي
        return; 
    }
    
    // إظهار شاشة التحميل
    window.showLoading(true);

    try {
        const response = await apiRequest("/api/codes/validate", 'POST', { 
            codeValue, 
            fieldId 
        });

        appliedCodeId = response.codeId;
        window.showLoading(false);
        
        let discountText = '';
        if (response.discountType === 'percent') {
            discountText = `خصم: ${response.discountValue}%`;
        } else if (response.discountType === 'fixed') {
            discountText = `خصم: ${response.discountValue} ج.م`;
        }
        
        resultContainer.innerHTML = `
            <div class="alert alert-success p-2 small m-0">
                ✅ ${response.message} (${discountText})
            </div>
        `;
        // يجب استدعاء دالة لتحديث ملخص الحجز وعرض المبلغ بعد الخصم
        updateBookingSummary(response); 

    } catch (error) {
        window.showLoading(false);
        appliedCodeId = null;
        resultContainer.innerHTML = `
            <div class="alert alert-danger p-2 small m-0">
                ❌ ${error.message || 'الكود غير صالح.'}
            </div>
        `;
        updateBookingSummary(null);
    }
}

// ===================================
// ⚽ تعديل دالة الحجز (bookStadium)
// ===================================
// يتم إرسال معرف الكود المطبق مع طلب الحجز

window.bookStadium = async function(fieldId, bookingDate, startTime, endTime) {
    // ... (جلب playersNeeded)
    
    const data = {
        fieldId,
        bookingDate,
        startTime,
        endTime,
        playersNeeded,
        codeId: appliedCodeId // <-- إضافة معرف الكود الذي تم التحقق منه
    };
    
    // ... (بقية منطق bookStadiumController الذي تم تعديله سابقاً)
    try {
        const response = await apiRequest("/api/booking/request", 'POST', data);
        // ... (منطق التوجيه للدفع أو عرض التأكيد)
    } catch (error) {
        // ...
    }
}

// ملاحظة: يجب إضافة حقل إدخال الكود (voucherCodeInput) ووعاء النتائج (voucherResult) وزر التحقق في واجهة الحجز الخاصة باللاعبين.

// new index (1).html (داخل وسم <script>)
// (تأكد من وجود دالة apiRequest في نطاق الوصول)

/**
 * تبدأ عملية الحجز وتتحقق من الحاجة لدفع عربون
 * يتم استدعاؤها عند النقر على ساعة متاحة
 */
window.bookStadium = async function(fieldId, bookingDate, startTime, endTime) {
    // يمكن إضافة مودال (Modal) هنا لجلب البيانات الإضافية
    // (سنفترض وجود عنصر لإدخال عدد اللاعبين الإضافيين)
    const playersNeededInput = document.getElementById('playersNeededInput');
    const playersNeeded = playersNeededInput ? parseInt(playersNeededInput.value) || 0 : 0; 
    
    // إظهار شاشة التحميل
    window.showLoading(true); 

    try {
        const response = await apiRequest("/api/booking/request", 'POST', {
            fieldId,
            bookingDate,
            startTime,
            endTime,
            playersNeeded
        });

        window.showLoading(false);
        
        if (response.requiresPayment) {
            // توجيه لصفحة الدفع لدفع العربون
            window.showToast("تم حجز الساعة مبدئياً. جاري التوجيه للدفع...", 'info');
            // تأكد أن /payment.html هو المسار الصحيح
            setTimeout(() => {
                window.location.href = `/payment.html?bookingId=${response.bookingId}`;
            }, 1000);
        } else {
            // حجز معلق لا يحتاج دفع عربون (أقل من 24 ساعة)
            window.showToast(response.message, 'info');
            // إعادة تحميل حالة الملعب لعرض حالة "Pending" الجديدة
            // (يجب أن تكون loadStadiumDetails مُعرّفة في هذا الملف)
            loadStadiumDetails(fieldId); 
        }
    } catch (error) {
        window.showLoading(false);
        window.showToast(`فشل الحجز: ${error.message}`, 'error');
    }
}
// تأكد من إضافة الدالة إلى appHelpers إذا كنت تستخدم نمط التصدير هذا
if (window.appHelpers) {
    window.appHelpers.bookStadium = window.bookStadium;
}
